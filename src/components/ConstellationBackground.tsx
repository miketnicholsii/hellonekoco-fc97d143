import { memo } from "react";

// Constellation effect disabled for performance
// The canvas-based particle animation was causing performance issues
export const ConstellationBackground = memo(function ConstellationBackground() {
  return null;
});